services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: bsb-browser:latest
    network_mode: host
    ports:
      - "${WEB_PORT:-8000}:8000"
    env_file:
      - .env.docker
    restart: unless-stopped
    environment:
      DJANGO_SETTINGS_MODULE: SBtools.settings.docker
    volumes:
      - "${STATS_DATA_DIR:-./data}:/app/data"
    extra_hosts:
      - "host.docker.internal:host-gateway"
